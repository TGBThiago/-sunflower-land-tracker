rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Cada usuário só lê/escreve o próprio documento
    match /tracker/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == docId;
      // Acesso somente de migração ao documento legado (copiar e excluir)
      allow get, delete: if docId == 'main';
    }

    // Config do auto-save (horários editáveis no site)
    match /config/autosave {
      allow read, write: if request.auth != null;
    }

    // Deny everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
